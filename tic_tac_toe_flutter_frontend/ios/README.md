This Flutter app targets Android by default in CI. The iOS directory will be generated or configured by Flutter tooling as needed.
To create iOS platform files locally, run:
- flutter create .
from the app root and select iOS.
