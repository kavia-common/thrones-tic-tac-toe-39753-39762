# Flutter App Root

This directory is the root of the Flutter application:
- pubspec.yaml (flutter app)
- lib/ (Dart sources)
- android/ (Android host)
- test/ (widget tests)

To run locally:
- flutter pub get
- flutter analyze
- flutter test
- flutter run
