Run tests from the app root:
- flutter test -r expanded
- flutter test test/all_tests.dart -r expanded

From the repository root with wrapper:
- ./flutterw test -r expanded

Ensure Flutter SDK is available in your environment if not using the wrapper.
