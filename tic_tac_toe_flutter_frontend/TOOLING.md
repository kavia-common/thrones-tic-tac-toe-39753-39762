Tooling notes

- If CI cannot determine the Flutter project root, ensure the working directory is `tic_tac_toe_flutter_frontend`.
- Run:
  - flutter pub get
  - flutter analyze
  - flutter test
- The presence of `.metadata`, `pubspec.yaml`, and `analysis_options.yaml` at this level marks this as the Flutter project root.
