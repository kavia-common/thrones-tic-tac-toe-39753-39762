#!/usr/bin/env bash
# Echo only the relative path to the Flutter app's pubspec.yaml from repository root.
# Some analysis tools require this exact output format.
echo "tic_tac_toe_flutter_frontend/pubspec.yaml"
