/// This is not the Flutter app entry. The real app lives in:
/// tic_tac_toe_flutter_frontend/
/// Use the provided scripts to run analyze/tests from the app directory.
import 'dart:io';

void main() {
  stderr.writeln('Not a Flutter app entry. Use tic_tac_toe_flutter_frontend/lib/main.dart');
  exit(3);
}
