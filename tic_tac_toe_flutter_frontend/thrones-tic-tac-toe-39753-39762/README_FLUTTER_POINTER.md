This repository's Flutter app is located at:
- tic_tac_toe_flutter_frontend/

Common commands (from repo root):
- ./run_flutter_from_root.sh         # flutter pub get, analyze, test from inside the app
- make analyze && make test          # Makefile delegates into the app directory
- ./run_flutter_via_marker.sh        # uses FLUTTER_PROJECT_PATH marker for resolution
