CI/Automation: Flutter project root

Flutter app path:
- tic_tac_toe_flutter_frontend

Run from repository root:
- cd tic_tac_toe_flutter_frontend && flutter pub get && flutter analyze && CI=true flutter test -r expanded && flutter build apk
