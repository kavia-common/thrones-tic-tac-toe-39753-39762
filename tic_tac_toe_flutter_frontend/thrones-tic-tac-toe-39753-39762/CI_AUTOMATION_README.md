Automated systems: The Flutter app is under

- tic_tac_toe_flutter_frontend/

Invoke Flutter commands inside that directory, or call one of these from repository root:
- ./flutter_ci_entrypoint.sh
- ./run_ci_make.sh
- make analyze && make test

This repository also contains a root-level minimal wrapper for detection, but the authoritative code is inside the app directory above.
