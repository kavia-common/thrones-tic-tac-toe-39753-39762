This repository root is not a Flutter application.

The actual Flutter app lives in:
- tic_tac_toe_flutter_frontend/

Use one of these commands from the repo root:
- ./run_flutter_from_root.sh
- ./run_flutter_via_marker.sh
- make analyze && make test

Do NOT attempt to run `flutter` commands at the repository root; run them inside the app directory.
