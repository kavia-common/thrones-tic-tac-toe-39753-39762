This is a minimal iOS scaffold for the root-level Flutter wrapper app to aid CI/analyzers.
The real Flutter app project lives at:
- tic_tac_toe_flutter_frontend/

Build and run the real app from that directory using `flutter run`.
