import 'dart:io';

/// PUBLIC_INTERFACE
/// Prints the path to the Flutter app's primary entry file for tooling.
void main(List<String> args) {
  // Standard primary entrypoint used by Flutter is lib/main.dart; this project
  // uses lib/main.dart to import and run the App widget.
  stdout.writeln('tic_tac_toe_flutter_frontend/lib/main.dart');
}
