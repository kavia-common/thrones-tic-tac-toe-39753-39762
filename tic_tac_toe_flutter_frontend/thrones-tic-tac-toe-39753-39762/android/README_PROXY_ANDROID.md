This repository root contains a minimal Flutter proxy app for tooling detection.
The actual app sources live in:
- tic_tac_toe_flutter_frontend/

For building and running, prefer:
- cd tic_tac_toe_flutter_frontend && flutter pub get && flutter run
or use scripts:
- ./run_flutter_from_root.sh
- ./run_flutter_via_marker.sh
