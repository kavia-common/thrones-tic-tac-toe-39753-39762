Android build notes

- Ensure local.properties contains valid `sdk.dir` and `flutter.sdk` paths.
- Build debug: flutter build apk
- Build release: flutter build apk --release
- The Gradle wrapper version is managed under android/gradle/wrapper/gradle-wrapper.properties.
