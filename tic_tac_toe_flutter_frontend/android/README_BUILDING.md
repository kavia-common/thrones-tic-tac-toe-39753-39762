# Building Android (Flutter app)

From this directory:
- ./gradlew tasks
- cd .. (app root)
- flutter pub get
- flutter build apk

Project root:
- See ../../tic_tac_toe_flutter_frontend (contains pubspec.yaml)
