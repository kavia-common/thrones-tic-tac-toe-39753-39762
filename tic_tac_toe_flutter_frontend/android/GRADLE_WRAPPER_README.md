This Android project uses the Gradle wrapper managed by Flutter.

To build Android from the app root:
- cd ../
- flutter build apk

CI environment:
- The Flutter SDK provides the Gradle wrapper during build via Flutter tooling.
- No manual gradle wrapper changes are required.
