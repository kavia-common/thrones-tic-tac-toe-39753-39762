'use strict';
// This placeholder file helps CI and some analyzers detect a valid Flutter web project structure.
// In a real build, Flutter generates a bootstrap script that loads the compiled app.
document.addEventListener('DOMContentLoaded', function () {
  // no-op placeholder
});
