# Analyzer and Tests

This is the Flutter project root.

To analyze:
- flutter analyze

To test:
- flutter test

From workspace root:
- ../../run_flutter_app.sh analyze
- ../../run_flutter_app.sh test

Or use the root-level CI wrapper:
- ../../flutter_ci.sh analyze
- ../../flutter_ci.sh test
